import { useNavigate } from "react-router-dom";

//eventlistener looking for:
//  clickBody
//  When event happens: login modal pops up

function login () {
    const navigate = useNavigate();

    return (
        <div>
            
        </div>
    )
}